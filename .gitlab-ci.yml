# This is a good template, but disabled for now
# "Settings>General: Visibility, project features, permissions -> Repositry: CI"

image: mambaorg/micromamba:latest

variables:
    MAMBA: micromamba --yes --quiet

before_script:
    - $MAMBA install -c conda-forge -c bioconda snakemake

build:
    script:
        - snakemake --lint
        - snakemake -nc1